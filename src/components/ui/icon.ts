"use client"

import styled from "styled-components";

export const StyledIcon = styled.span`
    display: inline-block;
    margin-left: 0.25rem;
    width: 1rem;

    svg {
        width: 1rem;
        height: 1rem;
    }
`;